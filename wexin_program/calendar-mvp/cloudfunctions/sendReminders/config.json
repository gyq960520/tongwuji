{
  "permissions": {
    "openapi": ["subscribeMessage.send"]
  },
  "triggers": [
    {
      "name": "sendReminders15min",
      "type": "timer",
      "config": "0 0,15,30,45 * * * * *"
    }
  ]
}
